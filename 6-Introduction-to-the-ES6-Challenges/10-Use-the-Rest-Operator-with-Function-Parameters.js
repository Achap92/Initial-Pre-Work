/*Learned to use the rest operator(...) to give our functions the ability to
use a variable amount of parameters.*/
const sum = (function() {
  "use strict";
  return function sum(...args) {
    return args.reduce((a, b) => a + b, 0);
  };
})();
console.log(sum(1, 2, 3)); // 6
