/*Learned that string value are immutable in JavaScript. The only way to change
a string literal is to assign the variable with a new string.*/
// Setup
var myStr = "Jello World";

// Only change code below this line

myStr = "Hello World"; // Fix Me
