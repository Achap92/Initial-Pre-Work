/*Created a function that we can then reuse in our code. To invoke the function
we must call it after the function definition using the same name as the function
definition. */
// Example
function ourReusableFunction() {
  console.log("Heyya, World");
}

ourReusableFunction();

// Only change code below this line
function reusableFunction() {
    console.log("Hi World");
}

reusableFunction();
