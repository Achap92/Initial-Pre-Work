/*Used the inequality operator (!=) to determine if val is not equal to a numebr
specified. If it is not then we return true. If it is then we return false.*/
// Setup
function testNotEqual(val) {
  if (val != 99) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testNotEqual("bob");
