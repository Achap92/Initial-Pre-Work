/*Added two parameters to a function then passed in arguments to assign the
parameters to those arguments.*/
// Example
function ourFunctionWithArgs(a, b) {
  console.log(a - b);
}
ourFunctionWithArgs(10, 5); // Outputs 5

// Only change code below this line.

function functionWithArgs(i, j) {
  console.log(i + j);
}

functionWithArgs(3, 7);
